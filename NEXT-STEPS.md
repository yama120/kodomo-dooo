# 明日やること（2026-08-02）

昨夜（8/1）に地域ページ230枚・robots.txt・sitemap.xml を入れて本番反映済みです。
以下は**あなたにしかできない作業**を上から順に並べています。

---

## 1. Google Search Console に登録する（最優先・15分）

これをやらないと、作った230ページが実際にインデックスされているか分かりません。
次の打ち手（クラブ詳細の静的化など）の優先順位も決められません。

1. https://search.google.com/search-console にアクセス
2. 「URLプレフィックス」を選び `https://chibispo.com` を入力
3. 所有権の確認方法で **「HTMLファイル」** を選ぶ
   → `googleXXXXXXXX.html` のようなファイルがダウンロードされます
4. **そのファイル名（または中身）を私に伝えてください。** 設置とデプロイはこちらでやります
   - metaタグ方式でも構いません。その場合は `<meta name="google-site-verification" content="...">` の中身を教えてください
5. 確認が通ったら「サイトマップ」に `sitemap.xml` を送信（これもこちらでやれます）

**GitHub Pages なのでDNS認証は使えません。** ファイルかmetaタグを選んでください。

---

## 2. GitHub Actions で自動更新するか決める（5分）

いまは **クラブを承認するたびに手で流す必要があります**。

```bash
cd ~/kodomo-dooo-deploy && node build-area-pages.mjs && node build-sitemap.mjs && git add -A && git commit -m "地域ページ更新" && git push
```

流さないと、新しいクラブが地域ページにもサイトマップにも出ません。

自動化する場合は「毎日1回、変更があればコミット」の形になります。
リポジトリがPublicなので**GitHub Actionsは無料**です。
勝手に定期実行の設定を入れるのは避けたので、**やるかどうかだけ返事をください。** 実装はこちらでやります。

---

## 3. データの表記ゆれを直すか決める（10分・任意）

クラブ自身が入力した値なので、こちらでは直していません。

**複数の値を1つの欄に入れているクラブが2件**
- 市区町村が `南風原町、糸満市`（沖縄県）
- 種目が `体操、サッカー`（福岡県）

→ いまは生成側で分割して両方のページに載せています。動作に問題はありません。

**種目の表記ゆれ（それぞれ別ページになっています）**
- `体操` / `体操教室`
- `運動教室` / `運動基礎` / `運動能力向上スクール` / `キッズ運動スクール`

→ 統合すると1ページあたりの掲載数が増えて検索に強くなりますが、
クラブが自分で書いた言葉を変えることになります。統合するか、このままかを決めてください。

---

## 4. クラブ詳細の静的化をやるか決める（判断のみ）

いちばん効果が大きい残タスクですが、**URLが変わります**（`club.html?id=UUID` → `/club/xxx/`）。
アプリ・共有リンク・サイトマップに影響するので、Search Consoleでインデックス状況を見てからのほうが安全です。
急がないなら 1 のあとで判断してください。

---

## 昨夜やったこと（確認済み）

| 内容 | 状態 |
|---|---|
| 地域ページ230枚（都道府県22／市区町村66／県×種目63／市×種目78／入口1） | 本番稼働・全リンク切れ0・title/description/h1の重複0 |
| robots.txt / sitemap.xml（356URL） | 本番稼働 |
| search.html を noindex,follow に | 反映済み（絞り込み結果は地域ページと重複するため） |
| クラブ詳細に canonical・description・構造化データ | 反映済み（静的化までの繋ぎ） |
| フッター「地域から探す」を /area/ に | 全56ページからクロールの起点に |
| 主要15ページ＋地域ページ15件の応答 | すべて200・コンソールエラーなし |
| アプリ（chibispo-app） | 正常動作を確認 |

**クラブを承認したときは 2 のコマンドを忘れずに。**
